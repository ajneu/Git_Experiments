RM := rm -fr

DIRS := proj
TMP  := *~

.PHONY: clean
clean:
	@echo ".....removing the following directories: $(DIRS)"
	@echo ".....and temporary files ($(TMP))"
	@echo ".....with the following command..."
	$(RM) $(DIRS) *~
